# MySQL MCP Server Configuration
# Copy this file to .env and configure your databases

# Default database to connect to (use database IDs like db_1, db_2, etc.)
DEFAULT_DATABASE=db_1

# Security Settings
MAX_QUERY_RESULTS=1000
ALLOW_DATA_MODIFICATION=true
ALLOW_DROP=false
ALLOW_TRUNCATE=false
READ_ONLY_MODE=false

# Database 1 (Primary/Local)
DB_HOST_1=localhost
DB_PORT_1=3306
DB_USER_1=root
DB_PASSWORD_1=your_password_here
DB_NAME_1=your_database_name

# Database 2 (Example: Staging/Production)
# DB_HOST_2=staging.example.com
# DB_PORT_2=3306
# DB_USER_2=app_user
# DB_PASSWORD_2=staging_password
# DB_NAME_2=staging_database

# Add more databases as needed by incrementing the number:
# DB_HOST_3=..., DB_HOST_4=..., etc.

# Configuration Notes:
# - Each database requires at least DB_HOST_n, DB_USER_n, and DB_PASSWORD_n
# - DB_PORT_n defaults to 3306 if not specified
# - DB_NAME_n is optional (you can specify it in queries)
# - Connection pool and advanced settings are configured via constants in the code
# - The system automatically discovers all databases with numbered variables